using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace ResearchCSharp.ARRAYS_STRUCTS_STRINGS
{
    class Book
    {
        public string title;
        public string author;
    }

    class ArrayBookdatabase
    {
        public static void Main(String[] args)
        {
            int capacity = 1000;
            Book[] books = new Book[capacity];
            bool repeat = true;

            string option;
            int amount = 0;
            string search;
            bool found;

            do
            {
                
            } while (true);


        }
    }
}
